# ═══════════════════════════════════════════════════════
# Resume-Libre — Environment Variables
# Copy this file to .env and fill in real values
# ═══════════════════════════════════════════════════════

# ─── OpenRouter (AI generation) ─────────────────────
# Get a free key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Free model with reasoning capability
OPENROUTER_MODEL=openai/gpt-oss-120b:free

# ─── Supabase (Auth + Database) ─────────────────────
# Find these at: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project.supabase.co
# Public anon key — safe for frontend (exposed via VITE_ prefix)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# Service role key — BACKEND ONLY, never expose to frontend
# Bypasses Row-Level Security — treat like a database password
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── Frontend (Vite — exposed to browser) ───────────
# These must be prefixed with VITE_ to be available in the browser
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_API_URL=http://localhost:8000

# ─── Apify (LinkedIn profile scraping) ─────────────
# Get a token at https://console.apify.com/account/integrations
# Actor used: harvestapi/linkedin-profile-scraper
APIFY_API_TOKEN=
# LinkedIn session cookie — required for scraping to work
# Get from browser: F12 → Application → Cookies → linkedin.com → li_at
LINKEDIN_COOKIE=li_at=

# ─── LaTeX compilation (Tectonic) ───────────────────
# Path to tectonic binary (installed in Docker, or locally)
TECTONIC_PATH=tectonic
